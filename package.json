{
  "name": "bitlayer-aa-sdk",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "build:wagmi": "turbo run build --filter=example-with-wagmi",
    "build:particle": "turbo run build --filter=example-with-particle",
    "build:particle-wagmi": "turbo run build --filter=example-with-particle-wagmi",
    "build:": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.2.3",
    "typescript": "~5.6.2",
    "vite": "^5.4.10"
  },
  "packageManager": "pnpm@9.12.3"
}
